import { Container, Typography } from '@mui/material';

function CreateEmployee() {
   return (
      <Container maxWidth='md'>
         <Typography variant='h1'>CreateEmployee</Typography>
      </Container>
   );
}

export default CreateEmployee;
